apiVersion: v1
kind: Pod
metadata:
  name: projected-volume-demo
  namespace: volumes-lab
  labels:
    application: volume-lab
    environment: local
spec:
  volumes:
    - name: context
      projected:
        sources:
          - configMap:
              name: projected-config
              items:
                - key: application-name
                  path: application-name
          - secret:
              name: projected-secret
              items:
                - key: api-token
                  path: api-token
          - downwardAPI:
              items:
                - path: pod-name
                  fieldRef:
                    fieldPath: metadata.name
                - path: namespace
                  fieldRef:
                    fieldPath: metadata.namespace
                - path: labels
                  fieldRef:
                    fieldPath: metadata.labels
  containers:
    - name: reader
      image: busybox:1.36
      command:
        - sh
        - -c
        - |
          while true; do
            echo "Application: $(cat /context/application-name)"
            echo "Pod: $(cat /context/pod-name)"
            echo "Namespace: $(cat /context/namespace)"
            sleep 15
          done
      volumeMounts:
        - name: context
          mountPath: /context
          readOnly: true

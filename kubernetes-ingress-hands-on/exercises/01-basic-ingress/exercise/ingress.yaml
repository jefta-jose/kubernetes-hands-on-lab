apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: basic-ingress
  namespace: ingress-lab
spec:
  ingressClassName: nginx
  rules:
    - host: ________
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ________
                port:
                  number: ________
